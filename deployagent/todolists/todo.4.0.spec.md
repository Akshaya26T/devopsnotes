# All apps should use same docker network
(deployagentagent and mydocker app needs to run in same network)

how many apps can be run in single compose file?
how many instances can be utilized to run one app?

# Learn load balancing in docker
# ingress controller